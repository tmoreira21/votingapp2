	<!-- Main jumbotron for a primary marketing message or call to action -->
	<div class="container">
		<form>
		<span class="centered-text">
		<h2>Pool List</h2><br />
		<div class="list-group">
			{{# each pools}}
			  	<a href="/pool/{{this._id}}" class="list-group-item">{{this.descript.name}}</a><br />
			{{/each}}
		</div>
	</div>